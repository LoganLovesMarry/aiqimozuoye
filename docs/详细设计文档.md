# 详细设计文档

## 1. 核心类设计
### Student类
- 属性：id, name, email, password, major, resumeUrl
- 方法：register(), login(), uploadResume()

### Company类
- 属性：id, name, email, password, license, status
- 方法：register(), postJob(), reviewResume()

## 2. 接口设计
- POST /api/student/login
- GET /api/job/list
- POST /api/application/apply
- GET /api/admin/statistics/employmentRate