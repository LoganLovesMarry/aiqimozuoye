# 就业管理系统前端（HTML版本）

纯 HTML + CSS + JavaScript 实现的就业管理系统前端。

## 🚀 快速开始

### 方式一：直接打开

直接用浏览器打开 `index.html` 文件即可。

### 方式二：使用本地服务器（推荐）

由于浏览器的同源策略，建议使用本地服务器运行：

**使用 Python（如果已安装）：**

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

**使用 Node.js（如果已安装）：**

```bash
# 安装 http-server
npm install -g http-server

# 启动服务器
http-server -p 8000
```

**使用 VS Code：**

安装 "Live Server" 插件，右键点击 `index.html` 选择 "Open with Live Server"。

然后访问：http://localhost:8000

## 📁 项目结构

```
frontend-html/
├── index.html              # 首页
├── login.html              # 登录页
├── register.html           # 注册页
├── css/
│   └── style.css           # 全局样式
├── js/
│   ├── api.js              # API 调用封装
│   └── auth.js             # 认证工具函数
├── student/                # 学生页面
│   ├── dashboard.html
│   ├── jobs.html
│   ├── applications.html
│   └── profile.html
├── company/                # 企业页面
│   ├── dashboard.html
│   ├── jobs.html
│   ├── applications.html
│   └── profile.html
└── admin/                  # 管理员页面
    ├── dashboard.html
    ├── students.html
    ├── companies.html
    └── announcements.html
```

## ⚙️ 配置

确保后端服务运行在 `http://localhost:8080`。

如果后端地址不同，请修改 `js/api.js` 中的 `API_BASE_URL`：

```javascript
const API_BASE_URL = 'http://your-backend-url:port/api';
```

## 🧪 测试账号

确保后端已启动，使用以下账号测试：

**管理员**
- 用户名: `admin`
- 密码: `admin123`

**学生**
- 邮箱: `zhangsan@example.com`
- 密码: `admin123`

**企业**
- 邮箱: `hr@alibaba.com`
- 密码: `admin123`

## ✨ 功能特性

### 学生端
- ✅ 浏览职位列表
- ✅ 搜索职位
- ✅ 在线投递简历
- ✅ 查看投递进度
- ✅ 管理个人信息

### 企业端
- ✅ 发布职位
- ✅ 管理职位
- ✅ 查看收到的简历
- ✅ 筛选候选人
- ✅ 更新候选人状态

### 管理员端
- ✅ 查看系统统计
- ✅ 管理学生信息
- ✅ 审核企业认证
- ✅ 发布系统公告

## 🎨 技术栈

- **HTML5** - 页面结构
- **CSS3** - 样式和布局
- **原生 JavaScript** - 交互逻辑
- **Fetch API** - HTTP 请求
- **LocalStorage** - 本地数据存储

## 📝 开发说明

### API 调用

所有 API 调用都封装在 `js/api.js` 中：

```javascript
// 示例：获取职位列表
const jobs = await jobApi.getAll();

// 示例：学生登录
const result = await studentApi.login(email, password);
```

### 认证管理

认证相关功能在 `js/auth.js` 中：

```javascript
// 检查是否登录
if (!isAuthenticated()) {
  window.location.href = '/login.html';
}

// 获取当前用户
const user = getUser();

// 退出登录
logout();
```

### 页面保护

在需要登录的页面开头添加：

```javascript
// 检查是否为学生角色
if (!initPage('student')) {
  // 未登录或角色不匹配，会自动跳转
}
```

## 🔧 常见问题

### 1. CORS 错误

如果遇到 CORS 错误，确保：
- 后端已配置 CORS 允许前端域名
- 使用本地服务器运行（不要直接用 file:// 协议）

### 2. API 请求失败

检查：
- 后端服务是否已启动
- API_BASE_URL 配置是否正确
- 浏览器控制台查看具体错误信息

### 3. 登录后跳转失败

检查：
- LocalStorage 是否被浏览器禁用
- 浏览器控制台是否有 JavaScript 错误

## 📄 License

MIT License
