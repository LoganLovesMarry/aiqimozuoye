# HTML版前端使用说明

## ✅ 已创建的文件

我已经为你创建了纯 HTML + CSS + JavaScript 版本的前端，位于 `frontend-html` 目录。

### 📁 目录结构

```
frontend-html/
├── index.html              # 首页 ✅
├── login.html              # 登录页 ✅
├── register.html           # 注册页 ✅
├── css/
│   └── style.css           # 全局样式 ✅
├── js/
│   ├── api.js              # API 调用封装 ✅
│   └── auth.js             # 认证工具函数 ✅
├── student/                # 学生页面目录
│   └── dashboard.html      # 学生仪表板示例 ✅
├── company/                # 企业页面目录（待创建）
└── admin/                  # 管理员页面目录（待创建）
```

## 🚀 快速启动

### 1. 启动后端服务

确保你的 Spring Boot 后端已经启动在 `http://localhost:8080`

### 2. 启动前端

进入 `frontend-html` 目录，使用以下任一方式：

**方式一：Python HTTP 服务器**
```bash
cd frontend-html
python -m http.server 8000
```

**方式二：Node.js HTTP 服务器**
```bash
npm install -g http-server
cd frontend-html
http-server -p 8000
```

**方式三：VS Code Live Server**
- 安装 "Live Server" 插件
- 右键 `index.html` 选择 "Open with Live Server"

### 3. 访问系统

打开浏览器访问：`http://localhost:8000`

## 📝 如何创建其他页面

我已经创建了基础框架和示例页面。你可以参考 `student/dashboard.html` 的模式创建其他页面。

### 页面模板结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>页面标题</title>
  <link rel="stylesheet" href="../css/style.css">
</head>
<body>
  <!-- 导航栏 -->
  <nav class="navbar">
    <div class="container">
      <a href="../index.html" class="navbar-brand">就业管理系统</a>
      <div class="navbar-menu">
        <span>欢迎，<span class="user-name"></span></span>
        <button onclick="logout()" class="btn btn-danger">退出</button>
      </div>
    </div>
  </nav>

  <!-- 侧边栏布局 -->
  <div class="dashboard-layout">
    <aside class="sidebar">
      <ul class="sidebar-menu">
        <!-- 导航菜单项 -->
      </ul>
    </aside>

    <main class="main-content">
      <!-- 页面内容 -->
    </main>
  </div>

  <!-- 引入脚本 -->
  <script src="../js/api.js"></script>
  <script src="../js/auth.js"></script>
  <script>
    // 检查登录和权限
    if (!initPage('student')) { // 或 'company' 或 'admin'
      throw new Error('未授权访问');
    }

    // 页面逻辑
    async function loadData() {
      // 调用 API 加载数据
      const result = await jobApi.getAll();
      // 处理数据并显示
    }

    loadData();
  </script>
</body>
</html>
```

### 需要创建的页面清单

**学生端** (`student/` 目录)
- ✅ dashboard.html - 已创建
- ⬜ jobs.html - 职位列表
- ⬜ applications.html - 我的投递
- ⬜ profile.html - 个人信息

**企业端** (`company/` 目录)
- ⬜ dashboard.html - 企业仪表板
- ⬜ jobs.html - 职位管理
- ⬜ applications.html - 收到的简历
- ⬜ profile.html - 企业信息

**管理员端** (`admin/` 目录)
- ⬜ dashboard.html - 管理员仪表板
- ⬜ students.html - 学生管理
- ⬜ companies.html - 企业管理
- ⬜ announcements.html - 公告管理

## 🎯 API 调用示例

所有 API 都已封装在 `js/api.js` 中：

```javascript
// 学生登录
const result = await studentApi.login(email, password);

// 获取职位列表
const jobs = await jobApi.getAll();

// 投递职位
await applicationApi.apply(studentId, jobId);

// 创建职位
await jobApi.create({
  title: '职位名称',
  description: '职位描述',
  salary: '薪资',
  location: '地点',
  companyId: companyId,
  status: 1
});
```

## 🔐 认证管理

```javascript
// 检查是否登录
if (!isAuthenticated()) {
  window.location.href = '/login.html';
}

// 获取当前用户
const user = getUser();
const role = getUserRole();

// 保存登录信息
saveAuth(user, token, role);

// 退出登录
logout();

// 初始化页面（检查权限+显示用户信息）
initPage('student'); // 或 'company' 或 'admin'
```

## 🎨 样式类参考

```html
<!-- 按钮 -->
<button class="btn btn-primary">主要按钮</button>
<button class="btn btn-success">成功按钮</button>
<button class="btn btn-danger">危险按钮</button>
<button class="btn btn-secondary">次要按钮</button>

<!-- 表单 -->
<div class="form-group">
  <label class="form-label">标签</label>
  <input type="text" class="form-control">
</div>

<!-- 卡片 -->
<div class="card">
  <div class="card-header">
    <h3 class="card-title">标题</h3>
  </div>
  <div>内容</div>
</div>

<!-- 徽章 -->
<span class="badge badge-success">成功</span>
<span class="badge badge-warning">警告</span>
<span class="badge badge-danger">危险</span>
<span class="badge badge-info">信息</span>

<!-- 表格 -->
<table class="table">
  <thead>
    <tr><th>列1</th><th>列2</th></tr>
  </thead>
  <tbody>
    <tr><td>数据1</td><td>数据2</td></tr>
  </tbody>
</table>
```

## 📖 测试账号

**管理员**
- 用户名: `admin`
- 密码: `admin123`
- 访问: http://localhost:8000/login.html?role=admin

**学生**
- 邮箱: `zhangsan@example.com`
- 密码: `admin123`
- 访问: http://localhost:8000/login.html?role=student

**企业**
- 邮箱: `hr@alibaba.com`
- 密码: `admin123`
- 访问: http://localhost:8000/login.html?role=company

## 💡 开发建议

1. **复制示例页面**：参考 `student/dashboard.html` 创建其他页面
2. **使用浏览器开发工具**：F12 查看控制台日志和网络请求
3. **测试 API**：先在浏览器控制台测试 API 调用
4. **逐步开发**：先完成一个角色的所有页面，再复制修改到其他角色

## ⚠️ 注意事项

1. 必须使用 HTTP 服务器运行，不能直接打开 HTML 文件
2. 确保后端 CORS 配置允许前端域名
3. LocalStorage 用于存储登录信息，不要在生产环境使用
4. API 路径在 `js/api.js` 的 `API_BASE_URL` 中配置

## 🔧 常见问题

**Q: CORS 错误？**
A: 确保后端配置了 CORS，允许 `http://localhost:8000`

**Q: 登录后跳转失败？**
A: 检查浏览器控制台是否有错误，确认页面文件存在

**Q: API 请求失败？**
A: 检查后端是否启动，API_BASE_URL 是否正确

## 📚 下一步

1. 根据示例创建剩余的 HTML 页面
2. 测试所有功能
3. 根据需要调整样式
4. 添加更多交互功能

如有问题，请查看浏览器控制台的错误信息！
